# Flask Configuration
FLASK_ENV=production
FLASK_DEBUG=False
SECRET_KEY=your-secret-key-here-change-this-in-production

# Application Settings
APP_BASE_URL=https://www.badenleg.ch
ALLOWED_HOSTS=badenleg.ch,www.badenleg.ch

# Email Configuration (if using real SMTP in future)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=noreply@badenleg.ch
SMTP_PASSWORD=your-smtp-password
SMTP_FROM=noreply@badenleg.ch

# Rate Limiting (Redis recommended for production)
RATELIMIT_STORAGE_URL=memory://
# For production with Redis:
# RATELIMIT_STORAGE_URL=redis://localhost:6379

# Security Settings
SESSION_COOKIE_SECURE=True
SESSION_COOKIE_HTTPONLY=True
SESSION_COOKIE_SAMESITE=Lax
PERMANENT_SESSION_LIFETIME=3600

# CORS Settings (if needed)
CORS_ORIGINS=https://badenleg.ch

# Anonymity Settings
ANONYMITY_RADIUS_METERS=120

# Admin Contact
ADMIN_EMAIL=leg@sihliconvalley.ch

